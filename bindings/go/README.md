# Go Binding

The Go binding provides a backend client for the FastAPI control plane.

Current coverage:

- health and project state
- backend overview and native manifest
- toolchain discovery
- device registration and heartbeat
- telemetry ingestion and reads
- flash and OTA requests

Use it from `bindings/go/iotron.go`.
